class Solution {
    public int solution(String message) {
        int answer = 0;
//         문자열의 길이를 잰다
        int length = message.length();
         answer = length*2;
        
        return answer;
    }
}